liip_imagine:
    resolvers:
        default:
            web_path: ~

    filter_sets:
        cache: ~

        portfolio_webp:
            quality: 80
            format: webp
            filters:
                thumbnail: { size: [1200, 800], mode: inset }